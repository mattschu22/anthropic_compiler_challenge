{
  "stage": 3,
  "id": "03_fixed_layout",
  "name": "Specialized Fixed Layout",
  "kind": "scalar_scratch",
  "claim": "The target workload has stable memory layout and dimensions, so pointer/header work can be baked in.",
  "workload": {
    "forest_height": 10,
    "rounds": 16,
    "batch_size": 256
  },
  "builder_args": {
    "fixed_layout": true,
    "unroll_rounds": false
  }
}
